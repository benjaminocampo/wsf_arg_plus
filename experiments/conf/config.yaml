defaults:
  - _self_
  - llm: mistral-7B-small
  - experiment: checkworthiness_zero_shot

input:
  data_path: ../data/wsf_arg_plus_per_message.csv
  data_size: 1                  # This parameter is just for testing. Should be equal to 1
  run_name: null                   # Run name will be the same as the llm name
  uri_path: ./mlruns              # Results will be saved in the ./mlruns using mlflow